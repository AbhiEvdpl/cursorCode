# Figma Handoff Instructions

The actual `.fig` file should be rebuilt using the guidance below (Figma does not yet support headless export from this environment). Use this document alongside `docs/figma-design-spec.md` when creating the file in Figma.

1. **Frames**
   - Desktop: 1440 × 5000 px, 12-col grid (80px columns / 24px gutter / 120px margin).
   - Tablet: 1024 × 5200 px, 8-col grid (72px margin / 16px gutter).
   - Mobile: 375 × 5600 px, single column with 16px padding.
2. **Components**
   - Buttons, Icon Chips, Service Cards, Map Module, Navigation (desktop + mobile), Footer, Modal, Form Inputs.
3. **Auto Layout**
   - Apply auto-layout to hero content stack, nav, cards, facility chips, CTA banner, footer columns, and mobile drawer.
4. **Interaction Prototypes**
   - Sticky header overlay, mobile nav slide-in, CTA modal open/close, map pin tooltip.
5. **Component Library Page**
   - Include color styles, typography styles, spacing tokens, icons, and button variants.

> Once recreated, export the `.fig` file to replace this placeholder description.
